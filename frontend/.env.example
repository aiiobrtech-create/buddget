VITE_API_BASE_URL=/api
VITE_APP_NAME=BUDDGET

# true = dados fictícios no browser | ausente/false = API real (padrão)
VITE_USE_MOCK_API=false

# Supabase (opcional no browser — Storage/Realtime; auth e dados via API)
# Copie de Project Settings → API no painel Supabase
# VITE_SUPABASE_URL=https://SEU_REF.supabase.co
# VITE_SUPABASE_ANON_KEY=sua-anon-key
